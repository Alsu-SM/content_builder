import { PageElementType } from '../../Model';

export const elements = [PageElementType.Cover];
export const elementsNames = new Map<PageElementType, string>([
	[PageElementType.Cover, 'Cover'],
]);
